import React from 'react'

import Movie from './Movie'

const MovieCard = props => {
  return  (
  
    <div className = "">
    <div>{`${props.movie}`}</div>;
    </div>
};

export default MovieCard;
