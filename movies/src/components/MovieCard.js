import React from 'react';
import ReactDOM from 'react-dom';

const MovieCard = ({ match }) => {
  return (
    <div> The page we are at is {match.params.id}</div>
  );
};

const MovieCards = () => {
  <ul>
    
}